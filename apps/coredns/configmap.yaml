apiVersion: v1
kind: ConfigMap
metadata:
  name: coredns
  namespace: coredns
data:
  Corefile: |
    .:53 {
        forward . 8.8.8.8
        errors
        log

        template IN A homelab.fritz.box {
            answer "{{ .Name }} 60 IN A 192.168.1.80"
        }
    }
